你输入密钥直接提示无效，99% 是因为**后端的 `authController.js` 还没有完全改完**，或者**旧的 Session 数据残留**导致的。

请按照以下 3 步排查，保证 100% 解决问题：

### 🛠️ 第一步：彻底覆盖完整的 `authController.js`

你可能在上一轮修改中，只注意到了顶部的 `SELECT` 逻辑，而忽略了我们换成了 SHA-256 后，整个函数的后续逻辑（比如去掉 `for` 循环和 `bcrypt.compare`）。

请打开后端的 `src/controllers/authController.js`，用下面这份**100% 完美的 SHA-256 版代码**完全覆盖：

```javascript
const crypto = require("crypto");

async function verifyKey(req, res) {
  const { key } = req.body;
  if (!key) return res.status(400).json({ error: "请提供访问密钥" });

  try {
    // 1. 将用户输入的明文密钥瞬间进行 SHA-256 哈希
    const inputHash = crypto.createHash("sha256").update(key).digest("hex");

    // 2. 利用 MySQL 索引直接 O(1) 精准秒查未使用的或在生命周期内的门票
    const [keys] = await pool.query(
      `SELECT id, key_hash, folder_name, duration_minutes, status, used_at
       FROM access_keys
       WHERE key_hash = ? 
         AND (expires_at IS NULL OR expires_at > NOW())
         AND (
           status = 'unused' 
           OR 
           (status = 'used' AND DATE_ADD(used_at, INTERVAL duration_minutes MINUTE) > NOW())
         )`,
      [inputHash],
    );

    // 3. 没查到说明票不对、不存在、或者已经过了 24 小时寿命
    if (keys.length === 0) {
      return res.status(401).json({ error: "密钥无效、已过期或不存在" });
    }

    const matchedKey = keys[0];

    // 4. 动态计算本次登录下发的 Cookie 剩余寿命
    let remainingDurationMs;
    let finalExpiresAt;
    const durationMs = (matchedKey.duration_minutes || 1440) * 60 * 1000; // 默认 24 小时

    if (matchedKey.status === "unused") {
      // 首次激活，给满额寿命
      remainingDurationMs = durationMs;
      finalExpiresAt = new Date(Date.now() + remainingDurationMs);
    } else {
      // 再次登录，计算卡片还剩多少寿命
      const absoluteExpireTime = matchedKey.used_at.getTime() + durationMs;
      remainingDurationMs = absoluteExpireTime - Date.now();
      finalExpiresAt = new Date(absoluteExpireTime);
    }

    // 额外防御：如果计算出的剩余时间小于 0，说明刚刚过期
    if (remainingDurationMs <= 0) {
      return res.status(401).json({ error: "密钥已过期" });
    }

    const rawToken = generateToken();
    const tokenHash = hashToken(rawToken);

    const conn = await pool.getConnection();
    try {
      await conn.beginTransaction();

      // 5. 只有第一次使用时，才去扣除门票状态、记录初始激活时间
      if (matchedKey.status === "unused") {
        await conn.query(
          `UPDATE access_keys SET status = 'used', used_at = NOW() WHERE id = ?`,
          [matchedKey.id],
        );
      }

      // 无论新老会话，都在 sessions 表留档
      await conn.query(
        `INSERT INTO sessions (token_hash, type, related_key_id, expires_at, folder_name)
         VALUES (?, 'user', ?, ?, ?)`,
        [
          tokenHash,
          matchedKey.id,
          finalExpiresAt,
          matchedKey.folder_name || "public",
        ],
      );

      await conn.commit();
      conn.release();
    } catch (err) {
      await conn.rollback();
      conn.release();
      throw err;
    }

    // 6. 下发 Cookie
    res.cookie("session_token", rawToken, {
      httpOnly: true,
      sameSite: "none",
      secure: true,
      maxAge: remainingDurationMs,
    });

    return res.json({
      success: true,
      message:
        matchedKey.status === "unused"
          ? "验证成功，密钥已激活"
          : "欢迎回来，门票依然有效",
      expires_at: finalExpiresAt.toISOString(),
    });
  } catch (err) {
    console.error("[Auth] 验证失败:", err.message);
    return res.status(500).json({ error: "服务器内部错误" });
  }
}

// 别忘了导出
module.exports = { verifyKey };
```

---

### 🧹 第二步：清除一切旧垃圾，确保数据纯净

因为加密算法换了，数据库里绝对不能有之前用旧脚本生成的 `bcrypt` 密文混在里面。

请去 Sealos 数据库终端**严格执行以下两行清空命令**：

```sql
TRUNCATE TABLE access_keys;
TRUNCATE TABLE sessions;

```

---

### 🎟️ 第三步：重新生成并导入 SHA-256 密匙

现在运行你刚刚改好的极速发卡脚本：

```bash
node generate-key.js 5 PTE_ PTE

```

1. 复制控制台里打印出来的黄色 `INSERT INTO access_keys ...` 语句。
2. 粘贴进数据库执行。
3. **关键点**：用你终端里打印出来的绿色明文密钥（例如 `PTE_XXXXXX`）去前端登录。

### 💡 绝招：如果还提示无效，看一眼后端报错

如果做完以上三步还不行，请直接看一眼你**后端运行终端的控制台日志**，或者看 `Sealos` 里的后端 `Logs`。如果是 SQL 报错（比如提示某个字段不存在），把报错甩给我，我一眼就能看出是哪个数据库表结构没对齐！
