# Production backend URL — frontend and backend are on different subdomains,
# so we must use the full URL (same-site cross-origin, CORS configured).
REACT_APP_API_BASE=https://cjdfnwwofgct.sealosgzg.site
